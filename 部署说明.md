# 地址配置与内网部署

浏览器只访问前端地址。`/LineInfo`、`/api`、`/ngimages` 和 `/detectedimages` 由前端服务器转发到后端，包含登录、Token 刷新、缺陷列表和图片文件。

| 运行方式 | 地址配置文件 | 默认后端 |
| --- | --- | --- |
| `npm run dev` | `.env.development` | `http://192.168.31.141:7804` |
| `start.bat` / `npm run preview` | `.env.production` | `http://127.0.0.1:7804` |

## 开发

修改 `.env.development` 中的 `BACKEND_URL`，然后运行 `npm run dev`。换后端电脑时只需修改这一项并重启前端服务。

## 前后端同机部署

1. 确认服务器后端已启动，可以通过 `http://127.0.0.1:7804` 访问。
2. 将项目同步到服务器，包含 `package.json`、依赖、`src`、`vite.config.js`、`.env.production` 和 `start.bat`。不要只复制 `dist`；启动脚本会重新构建。
3. 双击 `start.bat`。脚本会进入项目目录、构建并启动 4173 端口，启动输出会显示实际后端代理地址及可访问的 Network 地址。
4. 内网其他电脑访问 `http://服务器实际内网IP:4173`，例如 `http://192.168.31.103:4173`。
5. 服务器防火墙允许所需内网访问 TCP 4173 端口；浏览器不需要直接连接后端 7804 端口。

服务器 IP 改变时，其他电脑改用新地址即可；同机部署的 `BACKEND_URL` 仍使用 `127.0.0.1`。若后端端口不是 7804，修改 `.env.production` 中的端口。

已有最新 `dist` 时可以运行 `npm run preview`，避免重复构建。修改 `BACKEND_URL` 后必须重启前端服务；仅更改代理目标不需要重新构建。4173 被占用时脚本会报错，不会悄悄改用另一个端口。

## 每台机器单独覆盖

可以创建 `.env.development.local` 或 `.env.production.local`，仅写入一行 `BACKEND_URL=http://主机:端口`，覆盖对应默认配置。这些本机文件已加入 Git 忽略列表。系统环境变量 `BACKEND_URL` 的优先级更高，排查地址时以启动日志为准。

不要使用 `VITE_BACKEND_URL` 或将服务器本机地址写入浏览器请求中；浏览器上的 `localhost` / `127.0.0.1` 指访问者自己的电脑。

## 图片与接口排查

原图应请求 `http://服务器IP:4173/ngimages/...`，检测图应请求 `http://服务器IP:4173/detectedimages/...`。如果列表能加载但图片不能，检查失败请求的地址、状态码和服务器代理日志。

这里的代理由 Vite 服务提供。若以后改成 IIS 或 Nginx 仅托管 `dist`，需要在那里配置 `/LineInfo`、`/api`、`/ngimages`、`/detectedimages` 的反向代理；单独复制静态文件不会携带代理配置。
